[package]
name = "zip-install"
version = "0.1.0"
edition = "2024"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true

[dependencies]
anyhow = "1.0.100"
eframe = "0.33.3"
zip = "7.0.0"

[dependencies.native-dialog]
version = "0.9.4"
features = [
    "windows_dpi_awareness",
    "windows_visual_styles"
]
